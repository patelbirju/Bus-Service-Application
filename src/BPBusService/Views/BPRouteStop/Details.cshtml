@model BPBusService.Models.RouteStop

@{
    ViewData["Title"] = $"Details for stop" + Context.Request.Cookies["busRouteCode"] + " - " + Context.Request.Cookies["routeName"];
}

<h2>@ViewData["Title"]</h2>

<div>
    <h4>RouteStop</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.OffsetMinutes)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.OffsetMinutes)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.RouteStopId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
